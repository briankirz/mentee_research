# To locate your interpreter or compiler, run "whereis [interpreter]"
# Note: "$@" is functionally-equivalent to "$1 $2 $3 ..."

# not sure if shell script intro is going to work somewhere else...
python3 hmm.py "$@"